import React from 'react';
import styling from './CSS/NavBar.css';

export const NavBar = (props) => {
    return (
        <div>
            <button className="press"><label>Press me</label></button>
        </div>
    );
}